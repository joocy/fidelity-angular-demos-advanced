import { Routes } from '@angular/router';

// This demo uses a single-page layout (no routing) to keep the folder structure
// as the primary teaching focus. Routes are declared here for reference only.
export const routes: Routes = [];
